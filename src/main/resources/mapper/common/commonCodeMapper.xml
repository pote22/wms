<?xml version="1.0" encoding="UTF-8"?>
  <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjlogistics.wms.common.mapper.CommonCodeMapper">
    <!-- 공통코드 : 차량 톤(무기) 목록 조회 -->
    <select id="selectVehicleTonList" resultType="Map">
        SELECT SYS_CD
             , SYS_CDNM
          FROM TB_COMM_CODE
         WHERE 1=1
           AND sys_md_cd = 'WM'			    -- WM 업무
           AND sys_mc_cd = 'WM10001'	  -- Ton 관리목록
           AND USE_YN    = 'Y'
    </select>

    <!-- 공통코드 : 차량 톤수 정합성 체크 -->
    <select id="selectTonClsCdCheck" resultType="String">
        SELECT SYS_CD
          FROM WMS.TB_COMM_CODE
         WHERE 1 = 1
           AND SYS_MD_CD = 'WM'         -- WM 
           AND SYS_MC_CD = 'WM10001'    -- 차량별 톤급 리스트
           AND SYS_CD    = #{tonClsCd}
           AND USE_YN    = 'Y'
    </select>
</mapper>