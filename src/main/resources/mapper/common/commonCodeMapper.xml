<?xml version="1.0" encoding="UTF-8"?>
  <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjlogistics.wms.common.mapper.CommonCodeMapper">
    <!-- 공통코드 : 차량 톤(무기) 목록 조회 -->
    <select id="selectCommonCodeList" resultType="Map">
        SELECT SYS_CD
             , SYS_CDNM
          FROM WMS.TB_COMM_CODE
         WHERE 1 = 1
           AND SYS_GRP_CD = #{ sys_grp_cd }   -- 그룹 코드
           AND USE_YN     = 'Y'
         ORDER BY SYS_SORT_SEQ, SYS_CD
    </select>

    <!-- 공통코드 : 차량 톤수 정합성 체크 -->
    <select id="selectCommonCodeCheck" resultType="String">
        SELECT SYS_CD
          FROM WMS.TB_COMM_CODE
         WHERE 1 = 1
           AND SYS_GRP_CD = #{ sys_grp_cd }   -- 그룹 코드
           AND SYS_CD     = #{ sys_cd }      -- 공통 코드
           AND USE_YN     = 'Y'             -- 사용여부 : Y (사용중인 코드 대상으로 검색)
    </select>

    <!-- 공통 : 품목 검색 팝업 조회 -->
    <select id="selectProdSearchList" parameterType="map" resultType="map">
        SELECT PROD_CD
             , PROD_NM
          FROM WMS.TB_ITEM
         WHERE 1 = 1
           AND SRVC_CD = #{srvcCd}
           AND WH_CD   = #{whCd}
           <if test="prodCd != null and prodCd != ''">
           AND PROD_CD LIKE '%' || #{prodCd} || '%'
           </if>
           AND USE_YN  = 'Y'
         ORDER BY PROD_CD
    </select>
</mapper>