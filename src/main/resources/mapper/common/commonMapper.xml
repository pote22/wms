<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjlogistics.wms.common.mapper.CommonMapper">

    <!-- 사용자 로그 등록 -->
    <insert id="insertUserLog" parameterType="map">
        INSERT INTO WMS.TB_USER_LOG (
            LOG_ID
          , USER_ID
          , PROG_ID
          , LOG_DTL
          , IP_ADDR
          , USER_AGENT
          , ACCESS_DATE
          , REG_ID
          , REG_DATE
        ) VALUES (
            #{logId}
          , #{userId}
          , #{progId}
          , #{logDtl}
          , #{ipAddr}
          , #{userAgent}
          , NOW()
          , #{userId}
          , NOW()
        )
    </insert>

</mapper>
